apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sgs-chat.fullname" . }}-test-connection"
  labels:
    {{- include "sgs-chat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sgs-chat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
