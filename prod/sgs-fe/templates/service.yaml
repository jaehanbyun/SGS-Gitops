apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgs-fe.fullname" . }}
  labels:
    {{- include "sgs-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sgs-fe.selectorLabels" . | nindent 4 }}
