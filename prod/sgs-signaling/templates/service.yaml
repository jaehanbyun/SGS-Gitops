apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgs-signaling.fullname" . }}
  labels:
    {{- include "sgs-signaling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sgs-signaling.selectorLabels" . | nindent 4 }}
