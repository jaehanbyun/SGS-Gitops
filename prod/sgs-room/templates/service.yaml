apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgs-room.fullname" . }}
  labels:
    {{- include "sgs-room.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sgs-room.selectorLabels" . | nindent 4 }}
