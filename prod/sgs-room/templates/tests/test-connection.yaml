apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sgs-room.fullname" . }}-test-connection"
  labels:
    {{- include "sgs-room.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sgs-room.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
