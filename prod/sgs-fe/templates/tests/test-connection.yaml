apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sgs-fe.fullname" . }}-test-connection"
  labels:
    {{- include "sgs-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sgs-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
